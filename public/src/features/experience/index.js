import angular from 'angular';
import ExperienceController from './experience.controller';

export default angular
    .module('cv.experience', [])
    .controller('ExperienceController', ExperienceController)
    .name;